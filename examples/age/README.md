# age example

Self-contained. Generates a throwaway [age](https://github.com/FiloSottile/age) identity, encrypts a small YAML, decrypts it back, and prints every step.

```sh
go run ./examples/age
```

No credentials required.
